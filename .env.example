# OpenAI API key — used by /api/bridges and /api/mirror
# Without this, the app runs in "sample mode" (pre-baked bridges from scenarios).
OPENAI_API_KEY=

# Optional: override the model (default: gpt-5.4-mini)
OPENAI_MODEL=gpt-5.4-mini
